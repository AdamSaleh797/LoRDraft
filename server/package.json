{
  "name": "lordraft-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "check-types": "tsc --noemit",
    "lint": "cd .. && npx eslint ./server",
    "start": "node dist/index.js",
    "start-prod": "node dist/index.js --port 80",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-tsc": "npx webpack --mode development --progress --config webpack.config.js",
    "build": "npm run check-types && npm run build-tsc && npm run lint"
  },
  "author": "",
  "license": "ISC"
}
