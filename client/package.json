{
  "name": "lordraft-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format": "",
    "lint": "",
    "build-react": "npx webpack --mode production --progress --config webpack.config.js",
    "build-public": "cp -r public/* ../static/",
    "build": "npm run build-react && npm run build-public"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
